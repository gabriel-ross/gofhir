package {{.Lower}}

import (
	"context"
	"net/http"

	"github.com/gabriel-ross/gofhir"
	"github.com/go-chi/chi"
	"github.com/go-chi/render"
	"google.golang.org/grpc/codes"
	"google.golang.org/grpc/status"
)

func (svc *Service) create{{.Upper}}() http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		w.Write([]byte("{{.Upper}} create called"))
	}
}

func (svc *Service) list{{.Upper}}s() http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		w.Write([]byte("{{.Upper}} list called"))
	}
}

func (svc *Service) get{{.Upper}}() http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		w.Write([]byte("{{.Upper}} get called"))
	}
}

func (svc *Service) update{{.Upper}}() http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		w.Write([]byte("{{.Upper}} update called"))
	}
}

func (svc *Service) delete{{.Upper}}() http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		w.Write([]byte("{{.Upper}} delete called"))
	}
}
